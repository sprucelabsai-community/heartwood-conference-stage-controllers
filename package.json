{
  "name": "@sprucelabs/heartwood-conference-stage-controllers",
  "version": "8.1.11",
  "files": [
    "build/**"
  ],
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "module": "./build/esm/index.js",
  "sideEffects": false,
  "scripts": {
    "build.ci": "yarn run build.tsc && yarn run build.resolve-paths && yarn run lint",
    "build.dist": "tsc --project tsconfig.dist.json && yarn build.resolve-paths && mv build esm && yarn build.esm-postbuild && yarn build.tsc && yarn build.resolve-paths && mv esm build/ && yarn clean.dist",
    "build.dev": "yarn run build.tsc --sourceMap ; yarn run resolve-paths.lint",
    "build.copy-files": "mkdir -p build && rsync -avzq --exclude='*.ts' ./src/ ./build/",
    "build.resolve-paths": "resolve-path-aliases --target build --patterns '**/*.js,**/*.d.ts'",
    "build.tsc": "yarn run build.copy-files && tsc",
    "build.esm-postbuild": "esm-postbuild --target esm --patterns '**/*.js'",
    "clean.dist": "rm -rf build/__tests__ build/esm/__tests__",
    "clean": "yarn run clean.build",
    "clean.all": "yarn run clean.dependencies && yarn run clean.build",
    "clean.build": "rm -rf build/",
    "clean.dependencies": "rm -rf node_modules/ package-lock.json yarn.lock",
    "fix.lint": "eslint --fix --cache '**/*.ts'",
    "lint": "eslint --cache '**/*.ts'",
    "lint.tsc": "tsc -p . --noEmit",
    "post.watch.build": "yarn run build.copy-files && yarn run build.resolve-paths",
    "rebuild": "yarn run clean.all && yarn install && yarn run build.dev",
    "update.dependencies": "yarn run clean.dependencies && yarn",
    "resolve-paths.lint": "yarn run build.resolve-paths ; yarn run lint",
    "test": "jest",
    "release": "semantic-release",
    "watch.build.dev": "tsc-watch --sourceMap --onCompilationComplete 'yarn run post.watch.build'",
    "watch.rebuild": "yarn run clean.all && yarn install && yarn run watch.build.dev",
    "watch.tsc": "tsc -w"
  },
  "devDependencies": {
    "@sprucelabs/esm-postbuild": "^9.0.13",
    "@sprucelabs/jest-json-reporter": "^10.0.18",
    "@sprucelabs/resolve-path-aliases": "^4.0.13",
    "@sprucelabs/semantic-release": "^6.0.0",
    "@sprucelabs/spruce-test-fixtures": "^76.2.0",
    "@sprucelabs/test": "^11.1.0",
    "@sprucelabs/test-utils": "^7.2.1",
    "@types/node": "^25.0.3",
    "chokidar-cli": "^3.0.0",
    "eslint": "^9.39.2",
    "eslint-config-spruce": "^11.2.26",
    "jest": "^30.2.0",
    "jest-circus": "^30.2.0",
    "prettier": "^3.7.4",
    "ts-node": "^10.9.2",
    "tsc-watch": "^7.2.0",
    "typescript": "^5.9.3"
  },
  "description": "Video conferencing support right in Spruce!",
  "skill": {
    "namespace": "conference-stage-controllers",
    "version": "v2025_11_10",
    "upgradeIgnoreList": [
      "@sprucelabs/mercury-client",
      "@sprucelabs/mercury-types",
      "@sprucelabs/mercury-core-events",
      "@sprucelabs/spruce-event-plugin",
      "@sprucelabs/spruce-permission-plugin",
      "@sprucelabs/spruce-skill-utils",
      "@sprucelabs/spruce-view-plugin"
    ]
  },
  "jest": {
    "testRunner": "jest-circus/runner",
    "maxWorkers": 4,
    "testTimeout": 120000,
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "<rootDir>/tmp/",
      "<rootDir>/src/",
      "<rootDir>/node_modules/",
      "<rootDir>/build/__tests__/testDirsAndFiles/"
    ],
    "testMatch": [
      "**/__tests__/**/*.test.js?(x)"
    ],
    "moduleNameMapper": {
      "^#spruce/(.*)$": "<rootDir>/build/.spruce/$1"
    }
  },
  "dependencies": {
    "@sprucelabs/error": "^8.1.1",
    "@sprucelabs/heartwood-view-controllers": "^128.8.1",
    "@sprucelabs/schema": "^33.1.1",
    "@sprucelabs/spruce-core-schemas": "^42.1.1"
  }
}
